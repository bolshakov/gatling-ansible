# see Vagrant file and unsure it provision the same hasts
[workers]
workers-[1:2] ansible_ssh_common_args="-F ./ansible/files/ssh_config"

[local]
localhost

[workers:vars]
simulation_name=computerdatabase.BasicSimulation
